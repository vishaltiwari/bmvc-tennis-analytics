classdef Tracklet
    properties
        k_min; % Min frame number supporting the support candidate.
        k_max; % Max frame number containing the support candidate.
        
        %model;
        model = Model;
        
    end
end
